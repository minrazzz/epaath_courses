import React from "react";
import Button from "../components/Button";

const MathTestScreen = ({ title, description, image }) => {
  return (
    <div>
      {title} MathTestScreen
      <Button />
    </div>
  );
};

export default MathTestScreen;
