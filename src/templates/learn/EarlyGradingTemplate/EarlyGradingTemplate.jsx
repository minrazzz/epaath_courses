import React from "react";

const EarlyGradingTemplate = ({ children, ...props }) => {
  return (
    <div>
      Early grading template
      {children}
    </div>
  );
};

export default EarlyGradingTemplate;
